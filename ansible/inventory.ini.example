# Copy this file to inventory.ini and replace with your Terraform output IP
# Get the IP with: terraform output instance_public_ip

[app_servers]
app_server ansible_host=REPLACE_WITH_EC2_PUBLIC_IP ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa ansible_ssh_common_args='-o StrictHostKeyChecking=no'
